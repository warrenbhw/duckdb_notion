# name: test/sql/notion.test
# description: test notion extension
# group: [notion]

# Before we load the extension, this will fail
statement error
SELECT notion('Sam');
----
Catalog Error: Scalar Function with name notion does not exist!

# Require statement will ensure this test is run with this extension loaded
require notion

# Confirm the extension works
query I
SELECT notion('Sam');
----
Notion Sam 🐥

query I
SELECT notion_openssl_version('Michael') ILIKE 'Notion Michael, my linked OpenSSL version is OpenSSL%';
----
true
